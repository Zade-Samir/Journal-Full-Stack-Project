# Copy this file to .env and fill in your values for local development.
# Copy to .env.production and fill in for production.
# NEVER commit .env or .env.production to git.

# Main API Gateway URL
VITE_API_BASE_URL=http://localhost:8080

# Auth Service URL (direct — for Google OAuth2 flow)
VITE_AUTH_BASE_URL=http://localhost:8082
